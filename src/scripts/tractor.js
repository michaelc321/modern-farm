import {usePlants} from "./fields.js"

export const plantSeeds = (yearsPlan) => {
    for (const i = 0; i < usePlants.length; i++) {
        const plants = usePlants()
        Array.push(plants[i])
    }
}